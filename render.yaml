services:
  - type: web
    name: trsv-design-studio
    runtime: node
    buildCommand: npm install && npm run render:build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
