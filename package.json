{
  "name": "trsv-design-studio",
  "version": "1.0.0",
  "description": "TRSV Design Studio – Supporter Campaign Generator",
  "main": "server/index.js",
  "scripts": {
    "dev": "npx -y concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "node server/index.js",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "node server/index.js",
    "install:all": "npm install --include=dev && npm install --include=dev --prefix client",
    "render:build": "npm install --include=dev --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.12.0",
    "sqlite3": "^5.1.7"
  }
}
